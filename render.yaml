services:
  - type: web
    name: techtools
    env: python

    buildCommand: pip install -r requirements.txt
                  apt-get update && apt-get install -y ffmpeg
    startCommand: gunicorn run:app --workers 2 --timeout 180
